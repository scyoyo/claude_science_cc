# Virtual Lab - Production Environment Variables
# Copy to .env and fill in real values before running docker-compose.prod.yml

# Database password for PostgreSQL
DB_PASSWORD=change-me-strong-password

# Secret for encrypting stored API keys (LLM provider keys)
ENCRYPTION_SECRET=change-me-use-openssl-rand-hex-32

# JWT secret for authentication tokens
JWT_SECRET=change-me-use-openssl-rand-hex-32

# Generate secrets with:
#   openssl rand -hex 32
